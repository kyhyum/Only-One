using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RandomDrop : MonoBehaviour
{
   /* public void dropitem(string type)
    {
        int num = 0;
        switch (type)
        {
            case "Slime":
                num = Random.Range(0,15);
                break;
            case "TurtleShell":
                num = Random.Range(5,30);
                break;
            case "FreeLich":
                num = Random.Range(10,30);
                break;
        }

        if (num > 0 && num < 10)
        {
            GameObject dropitem = objectmanager.makeObj("itemPenta");
            dropitem.transform.position += this.transform.position;

        }
        else if (num >= 10 && num < 20)
        {
            GameObject dropitem = objectmanager.makeObj("itemHexagon");
            dropitem.transform.position += this.transform.position;
        }
        else if (num >= 20 && num <= 25)
        {
            GameObject dropitem = objectmanager.makeObj("itemHeart");
            dropitem.transform.position += this.transform.position;

        }
        else if (num > 25 && num < 30)
        {
            GameObject dropitem = objectmanager.makeObj("itemDiamond");
            dropitem.transform.position += this.transform.position;
        }
    }*/
    
}
